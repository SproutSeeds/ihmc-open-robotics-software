/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package controller_msgs.msg.dds;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
This message describes the feedback gain computed by the solver.

A state-feedback gain is used to apply state feedback control.
Generally speaking, this is a matrix defined by the dimension of state and control spaces.
Both dimensions need to described in nx and nu, respectively.

The values of this gain are stored in data array.
The entries of the matrix are stored in row-major order.
The entire first row is stored first, followed by the entire second row, and so on.
<p>Source (controller_msgs/CrocoddylFeedbackGainMessage):
<pre>{@code
# This message describes the feedback gain computed by the solver.
#
# A state-feedback gain is used to apply state feedback control.
# Generally speaking, this is a matrix defined by the dimension of state and control spaces.
# Both dimensions need to described in nx and nu, respectively.
#
# The values of this gain are stored in data array.
# The entries of the matrix are stored in row-major order.
# The entire first row is stored first, followed by the entire second row, and so on.

# This represents the dimension of the state and control
uint32 nx # number of columns
uint32 nu # number of rows

# This represents the entries of the matrix allocated by row order.
float64[] data}</pre>
*/
public class CrocoddylFeedbackGainMessage implements ROS2Message<CrocoddylFeedbackGainMessage>
{
   public static final java.lang.String name = "controller_msgs::msg::dds_::CrocoddylFeedbackGainMessage_";

   /**
      This represents the dimension of the state and control
   */
   private int nx_; // number of columns
   private int nu_; // number of rows
   /**
      This represents the entries of the matrix allocated by row order.
   */
   private final IDLDoubleSequence data_;

   public CrocoddylFeedbackGainMessage()
   {
      data_ = new IDLDoubleSequence();

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // nx_
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // nu_
      currentAlignment += data_.calculateSizeBytes(currentAlignment);

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      buffer.writeInt(nx_);
      buffer.writeInt(nu_);
      data_.serialize(buffer);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      nx_ = buffer.readInt();
      nu_ = buffer.readInt();
      data_.deserialize(buffer);

   }

   @Override
   public void set(CrocoddylFeedbackGainMessage from)
   {
      nx_ = from.nx_;
      nu_ = from.nu_;
      data_.set(from.data_);

   }

   public int getNx()
   {
      return nx_;
   }

   public void setNx(int nx_)
   {
      this.nx_ = nx_;
   }

   public int getNu()
   {
      return nu_;
   }

   public void setNu(int nu_)
   {
      this.nu_ = nu_;
   }

   public IDLDoubleSequence getData()
   {
      return data_;
   }


}
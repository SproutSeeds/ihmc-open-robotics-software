/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package controller_msgs.msg.dds;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
This is a message that holds data to describe the control.

The control is defined by:
* the type of control input,
* the type of control parametrization,
* the control vector, and
* the feedback gain

This information allow us to compute the control in a desired time.
The order of the control is designed to be consistent with the URDF.
<p>Source (controller_msgs/CrocoddylControlMessage):
<pre>{@code
# This is a message that holds data to describe the control.
#
# The control is defined by:
#  * the type of control input,
#  * the type of control parametrization,
#  * the control vector, and
#  * the feedback gain
#
# This information allow us to compute the control in a desired time.
# The order of the control is designed to be consistent with the URDF.

# Type of control inputs
uint8 EFFORT = 0
uint8 ACCELERATION_CONTACTFORCE = 1

# Type of control parametrization
uint8 POLYZERO = 0
uint8 POLYONE = 1
uint8 POLYTWO = 2

uint8 input
uint8 parametrization

# This represents the control input vector
float64[] u

# This represents the feedback gain
CrocoddylFeedbackGainMessage gain}</pre>
*/
public class CrocoddylControlMessage implements ROS2Message<CrocoddylControlMessage>
{
   public static final java.lang.String name = "controller_msgs::msg::dds_::CrocoddylControlMessage_";

   /**
      Type of control inputs
   */
   public static final byte EFFORT = 0;
   public static final byte ACCELERATION_CONTACTFORCE = 1;
   /**
      Type of control parametrization
   */
   public static final byte POLYZERO = 0;
   public static final byte POLYONE = 1;
   public static final byte POLYTWO = 2;
   private byte input_;
   private byte parametrization_;
   /**
      This represents the control input vector
   */
   private final IDLDoubleSequence u_;
   /**
      This represents the feedback gain
   */
   private final controller_msgs.msg.dds.CrocoddylFeedbackGainMessage gain_;

   public CrocoddylControlMessage()
   {
      u_ = new IDLDoubleSequence();
      gain_ = new controller_msgs.msg.dds.CrocoddylFeedbackGainMessage();

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // input_
      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // parametrization_
      currentAlignment += u_.calculateSizeBytes(currentAlignment);
      currentAlignment += gain_.calculateSizeBytes(currentAlignment);

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      buffer.writeByte(input_);
      buffer.writeByte(parametrization_);
      u_.serialize(buffer);
      gain_.serialize(buffer);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      input_ = buffer.readByte();
      parametrization_ = buffer.readByte();
      u_.deserialize(buffer);
      gain_.deserialize(buffer);

   }

   @Override
   public void set(CrocoddylControlMessage from)
   {
      input_ = from.input_;
      parametrization_ = from.parametrization_;
      u_.set(from.u_);
      gain_.set(from.gain_);

   }

   public byte getInput()
   {
      return input_;
   }

   public void setInput(byte input_)
   {
      this.input_ = input_;
   }

   public byte getParametrization()
   {
      return parametrization_;
   }

   public void setParametrization(byte parametrization_)
   {
      this.parametrization_ = parametrization_;
   }

   public IDLDoubleSequence getU()
   {
      return u_;
   }

   public controller_msgs.msg.dds.CrocoddylFeedbackGainMessage getGain()
   {
      return gain_;
   }


}
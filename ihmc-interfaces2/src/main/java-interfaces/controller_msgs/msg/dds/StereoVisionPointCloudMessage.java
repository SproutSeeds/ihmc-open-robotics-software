/*
 * This class was automatically generated by jros2.
 * Do not modify this file directly.
 */
package controller_msgs.msg.dds;

import us.ihmc.fastddsjava.cdr.CDRBuffer;
import us.ihmc.fastddsjava.cdr.idl.*;
import us.ihmc.jros2.ROS2Message;

/**
Should disappear for the ROS equivalent.
<p>Source (controller_msgs/StereoVisionPointCloudMessage):
<pre>{@code
# Should disappear for the ROS equivalent.

# Unique ID used to identify this message, should preferably be consecutively increasing.
uint64 sequence_id

int64 timestamp
geometry_msgs/Point sensor_position
geometry_msgs/Quaternion sensor_orientation
# Specifies whether the pointcloud is expressed in the local coordinate system of the sensor or expressed in world frame.
bool is_data_local_to_sensor false

# There are two types of confidence value noticing the quality of the data for sensor pose and point cloud.
# The range of confidence is from 0.0 with the worst quality to 1.0 with the best quality.
# The confidence of the sensor pose represents the quality of the pose estimation.
float64 sensor_pose_confidence 1.0
# There are two types of confidence value noticing the quality of the data for sensor pose and point cloud.
# The range of confidence is from 0.0 with the worst quality to 1.0 with the best quality.
# The confidence of the point cloud represents the quality of the collected point cloud data.
float64 point_cloud_confidence 1.0

# The center location of the bounding box of all the points.
# The location of each point in the pointcloud is with respect to this location.
geometry_msgs/Point point_cloud_center
# The pointcloud is down to a given resolution defined in meters.
float64 resolution
# The number of points in this frame.
int32 number_of_points
# The pointcloud. The points are stored as 16-bits signed integers.
# To retrieve the actual coordinate use: "(point_cloud.get() + 0.5) * resolution".
# The size of this byte buffer is: "number_of_points * 3 * 2".
# See us.ihmc.robotEnvironmentAwareness.communication.converters.StereoPointCloudCompression for more info on the compression protocol.
byte[<=20000000] point_cloud
# The compressed colors.
# The color for each point in the pointcloud. The colors are stored as 3 bytes: (R, G, B).
# See us.ihmc.robotEnvironmentAwareness.communication.converters.StereoPointCloudCompression for more info on the compression protocol.
byte[<=7000000] colors
# Whether point_cloud and colors have been compressed using the LZ4Compression or not.
bool lz4_compressed}</pre>
*/
public class StereoVisionPointCloudMessage implements ROS2Message<StereoVisionPointCloudMessage>
{
   public static final java.lang.String name = "controller_msgs::msg::dds_::StereoVisionPointCloudMessage_";

   /**
      Unique ID used to identify this message, should preferably be consecutively increasing.
   */
   private long sequence_id_;
   private long timestamp_;
   private final geometry_msgs.msg.dds.Point sensor_position_;
   private final geometry_msgs.msg.dds.Quaternion sensor_orientation_;
   /**
      Specifies whether the pointcloud is expressed in the local coordinate system of the sensor or expressed in world frame.
   */
   private boolean is_data_local_to_sensor_;
   /**
      There are two types of confidence value noticing the quality of the data for sensor pose and point cloud.
      The range of confidence is from 0.0 with the worst quality to 1.0 with the best quality.
      The confidence of the sensor pose represents the quality of the pose estimation.
   */
   private double sensor_pose_confidence_;
   /**
      There are two types of confidence value noticing the quality of the data for sensor pose and point cloud.
      The range of confidence is from 0.0 with the worst quality to 1.0 with the best quality.
      The confidence of the point cloud represents the quality of the collected point cloud data.
   */
   private double point_cloud_confidence_;
   /**
      The center location of the bounding box of all the points.
      The location of each point in the pointcloud is with respect to this location.
   */
   private final geometry_msgs.msg.dds.Point point_cloud_center_;
   /**
      The pointcloud is down to a given resolution defined in meters.
   */
   private double resolution_;
   /**
      The number of points in this frame.
   */
   private int number_of_points_;
   /**
      The pointcloud. The points are stored as 16-bits signed integers.
      To retrieve the actual coordinate use: "(point_cloud.get() + 0.5) * resolution".
      The size of this byte buffer is: "number_of_points * 3 * 2".
      See us.ihmc.robotEnvironmentAwareness.communication.converters.StereoPointCloudCompression for more info on the compression protocol.
   */
   private final IDLByteSequence point_cloud_;
   /**
      The compressed colors.
      The color for each point in the pointcloud. The colors are stored as 3 bytes: (R, G, B).
      See us.ihmc.robotEnvironmentAwareness.communication.converters.StereoPointCloudCompression for more info on the compression protocol.
   */
   private final IDLByteSequence colors_;
   /**
      Whether point_cloud and colors have been compressed using the LZ4Compression or not.
   */
   private boolean lz4_compressed_;

   public StereoVisionPointCloudMessage()
   {
      sensor_position_ = new geometry_msgs.msg.dds.Point();
      sensor_orientation_ = new geometry_msgs.msg.dds.Quaternion();
      is_data_local_to_sensor_ = (boolean) false;
      sensor_pose_confidence_ = (double) 1.0;
      point_cloud_confidence_ = (double) 1.0;
      point_cloud_center_ = new geometry_msgs.msg.dds.Point();
      point_cloud_ = new IDLByteSequence(20000000);
      colors_ = new IDLByteSequence(7000000);
      lz4_compressed_ = (boolean) false;

   }

   @Override
   public int calculateSizeBytes(int currentAlignment)
   {
      int initialAlignment = currentAlignment;

      currentAlignment += 8 + CDRBuffer.alignment(currentAlignment, 8); // sequence_id_
      currentAlignment += 8 + CDRBuffer.alignment(currentAlignment, 8); // timestamp_
      currentAlignment += sensor_position_.calculateSizeBytes(currentAlignment);
      currentAlignment += sensor_orientation_.calculateSizeBytes(currentAlignment);
      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // is_data_local_to_sensor_
      currentAlignment += 8 + CDRBuffer.alignment(currentAlignment, 8); // sensor_pose_confidence_
      currentAlignment += 8 + CDRBuffer.alignment(currentAlignment, 8); // point_cloud_confidence_
      currentAlignment += point_cloud_center_.calculateSizeBytes(currentAlignment);
      currentAlignment += 8 + CDRBuffer.alignment(currentAlignment, 8); // resolution_
      currentAlignment += 4 + CDRBuffer.alignment(currentAlignment, 4); // number_of_points_
      currentAlignment += point_cloud_.calculateSizeBytes(currentAlignment);
      currentAlignment += colors_.calculateSizeBytes(currentAlignment);
      currentAlignment += 1 + CDRBuffer.alignment(currentAlignment, 1); // lz4_compressed_

      return currentAlignment - initialAlignment;
   }

   @Override
   public void serialize(CDRBuffer buffer)
   {
      buffer.writeLong(sequence_id_);
      buffer.writeLong(timestamp_);
      sensor_position_.serialize(buffer);
      sensor_orientation_.serialize(buffer);
      buffer.writeBoolean(is_data_local_to_sensor_);
      buffer.writeDouble(sensor_pose_confidence_);
      buffer.writeDouble(point_cloud_confidence_);
      point_cloud_center_.serialize(buffer);
      buffer.writeDouble(resolution_);
      buffer.writeInt(number_of_points_);
      point_cloud_.serialize(buffer);
      colors_.serialize(buffer);
      buffer.writeBoolean(lz4_compressed_);

   }

   @Override
   public void deserialize(CDRBuffer buffer)
   {
      sequence_id_ = buffer.readLong();
      timestamp_ = buffer.readLong();
      sensor_position_.deserialize(buffer);
      sensor_orientation_.deserialize(buffer);
      is_data_local_to_sensor_ = buffer.readBoolean();
      sensor_pose_confidence_ = buffer.readDouble();
      point_cloud_confidence_ = buffer.readDouble();
      point_cloud_center_.deserialize(buffer);
      resolution_ = buffer.readDouble();
      number_of_points_ = buffer.readInt();
      point_cloud_.deserialize(buffer);
      colors_.deserialize(buffer);
      lz4_compressed_ = buffer.readBoolean();

   }

   @Override
   public void set(StereoVisionPointCloudMessage from)
   {
      sequence_id_ = from.sequence_id_;
      timestamp_ = from.timestamp_;
      sensor_position_.set(from.sensor_position_);
      sensor_orientation_.set(from.sensor_orientation_);
      is_data_local_to_sensor_ = from.is_data_local_to_sensor_;
      sensor_pose_confidence_ = from.sensor_pose_confidence_;
      point_cloud_confidence_ = from.point_cloud_confidence_;
      point_cloud_center_.set(from.point_cloud_center_);
      resolution_ = from.resolution_;
      number_of_points_ = from.number_of_points_;
      point_cloud_.set(from.point_cloud_);
      colors_.set(from.colors_);
      lz4_compressed_ = from.lz4_compressed_;

   }

   public long getSequenceId()
   {
      return sequence_id_;
   }

   public void setSequenceId(long sequence_id_)
   {
      this.sequence_id_ = sequence_id_;
   }

   public long getTimestamp()
   {
      return timestamp_;
   }

   public void setTimestamp(long timestamp_)
   {
      this.timestamp_ = timestamp_;
   }

   public geometry_msgs.msg.dds.Point getSensorPosition()
   {
      return sensor_position_;
   }

   public geometry_msgs.msg.dds.Quaternion getSensorOrientation()
   {
      return sensor_orientation_;
   }

   public boolean getIsDataLocalToSensor()
   {
      return is_data_local_to_sensor_;
   }

   public void setIsDataLocalToSensor(boolean is_data_local_to_sensor_)
   {
      this.is_data_local_to_sensor_ = is_data_local_to_sensor_;
   }

   public double getSensorPoseConfidence()
   {
      return sensor_pose_confidence_;
   }

   public void setSensorPoseConfidence(double sensor_pose_confidence_)
   {
      this.sensor_pose_confidence_ = sensor_pose_confidence_;
   }

   public double getPointCloudConfidence()
   {
      return point_cloud_confidence_;
   }

   public void setPointCloudConfidence(double point_cloud_confidence_)
   {
      this.point_cloud_confidence_ = point_cloud_confidence_;
   }

   public geometry_msgs.msg.dds.Point getPointCloudCenter()
   {
      return point_cloud_center_;
   }

   public double getResolution()
   {
      return resolution_;
   }

   public void setResolution(double resolution_)
   {
      this.resolution_ = resolution_;
   }

   public int getNumberOfPoints()
   {
      return number_of_points_;
   }

   public void setNumberOfPoints(int number_of_points_)
   {
      this.number_of_points_ = number_of_points_;
   }

   public IDLByteSequence getPointCloud()
   {
      return point_cloud_;
   }

   public IDLByteSequence getColors()
   {
      return colors_;
   }

   public boolean getLz4Compressed()
   {
      return lz4_compressed_;
   }

   public void setLz4Compressed(boolean lz4_compressed_)
   {
      this.lz4_compressed_ = lz4_compressed_;
   }


}